spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security_demo_test
    username: vcardozof
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQL5Dialect
